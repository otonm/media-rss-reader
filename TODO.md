use superpowers to implement the next changes:
1. autoscrolling works properly with videos, however the video stops auto scrolling on the lower edge of screen. this should happen on the upper edge.
2. gifs repeat indefinitely and autoscrolling does not resume
3. on the first run, all items have the "read" flag set and the checkmark on the upper right side, and when scrolling by, items do not get the "read" flag set. fix this. items should be marked as read, but still shown until the page gets reloaded or the "show read" option is toggled